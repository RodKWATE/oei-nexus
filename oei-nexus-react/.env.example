# Leave empty to use the Vite proxy (dev) or same-origin (prod behind a gateway)
VITE_API_URL=

# Set this in production when the API lives on a separate domain:
# VITE_API_URL=https://api.oei-nexus.example.com
