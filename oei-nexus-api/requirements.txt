# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
gunicorn==22.0.0           # Production WSGI process manager

# Database
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
psycopg2-binary==2.9.9     # Alembic sync migrations

# Migrations
alembic==1.13.1

# Validation
pydantic==2.7.1
pydantic-settings==2.2.1
email-validator==2.1.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9

# Cache / Async tasks
redis==5.0.4
celery[redis]==5.4.0

# HTTP client (for external APIs later)
httpx==0.27.0

# Observability
structlog==24.2.0
sentry-sdk[fastapi]==2.5.1

# Utils
python-dotenv==1.0.1

# Dev / Testing (not installed in production Docker image)
# pytest==8.2.0
# pytest-asyncio==0.23.6
# pytest-cov==5.0.0
# httpx  (already above — used as async test client)
